@keyframes glowFloat {
  0% {
    transform: translate(-40px, -30px) scale(0.95);
    opacity: 0.55;
  }
  50% {
    transform: translate(40px, 30px) scale(1.15);
    opacity: 1;
  }
  100% {
    transform: translate(-40px, -30px) scale(0.95);
    opacity: 0.55;
  }
}
